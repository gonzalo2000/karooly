<br>

<div class="booyah-box col-xs-10 col-xs-offset-1">
  <h1 class="text-center">Learn New Vocabulary!</h1>
  <hr>

  <% @lessons.each do |lesson| %>
    <h2><%= lesson.title %></h2>
    <i>by <%= lesson.user.email %> (<%= lesson.subject %>, Level <%= lesson.difficulty %>)</i>
    <p><%= lesson.description %></p>
  <% end %>
</div>