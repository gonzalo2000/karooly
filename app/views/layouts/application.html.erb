<!DOCTYPE html>
<html>
<head>
  <title><%= yield(:title) + " - Karooly" %></title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= favicon_link_tag 'favicon.ico' %>
  <meta name="description" content="Learn languages, increase your vocabulary.">
  <meta name="keywords" content="language, teaching, tool, hybrid, teacher, vocabulary, learn, words, concepts">
  <meta name="author" content="Gonzalo Gallardo">
</head>
<body>
  <%= render "layouts/navbar" unless @disable_nav %>

  <% if notice.present? %>
    <p class="alert alert-info"><%= notice %></p>
  <% end %>
  <% if alert.present? %>
    <p class="alert alert-danger"><%= alert %></p>
  <% end %>
  
  <%= yield %>

</body>
</html>
