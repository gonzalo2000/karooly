<br>
<div class ="booyah-box col-xs-10 col-xs-offset-1">
  <h1 class="text-center"><%= @lesson.title %></h1>
  <h2 class="text-center"><%= @lesson.subject %>, Level <%= @lesson.difficulty %></h2>
  <h4 class="text-center"><i>(you created this lesson)</i></h4>
  <br>
  <p class="text-center"><%= @lesson.description %></p>
  <br>
  <div class="text-center">
    <%= link_to 'Edit Lesson', edit_teacher_lesson_path(@lesson), class: 'btn btn-primary' %>
    <%= link_to 'Student View', lesson_path(@lesson), class: 'btn btn-warning' %>
    <%= link_to 'My Lessons', '#', class: 'btn btn-info' %>
  </div>
  
  <hr>
  
  <h3>Vocabulary in This Lesson</h3>
  <br>
  <div class="text-center">
    <%= link_to 'Add word', new_teacher_lesson_word_path(@lesson), class: 'btn btn-success btn-lg' %>
  </div>
  
  <br class="clear">
  <br>
  
  <% if @lesson.words.empty? %>
    <i>(this lesson doesn't have any vocabulary yet!)</i>
  <% end %>

  <ul>
    <% @lesson.words.each do |word| %>
      <li>
        <b><%= word.term %></b> --> <i><%= word.reference %></i> &nbsp
        <%= link_to 'Edit Word', edit_teacher_lesson_word_path(@lesson, word), class: 'btn btn-primary' %>
        <%= link_to 'Delete Word', teacher_lesson_word_path(@lesson, word), 
            method: :delete,
            data: { confirm: 'This will permanently delete this word. Are you sure?' },
            class: 'btn btn-danger' %>
      </li>
      <br>
    <% end %>
  </ul>
</div>