<br>

<div class="booyah-box col-xs-10 col-xs-offset-1">
  <h1 class="text-center">DASHBOARD (<i><%= current_user.username %></i>)</h1>
  <br>

  <h2>MY LESSONS</h2>
  <ul>
    <% current_user.lessons.each do |lesson| %>
      <li><%= link_to lesson.title, teacher_lesson_path(lesson) %>: <%= lesson.description %>
        <%= link_to 'Lesson Admin', teacher_lesson_path(lesson), class: 'btn btn-primary btn-sm' %> &nbsp
        <%= link_to 'Delete Lesson', teacher_lesson_path(lesson), 
            method: :delete,
            data: { confirm: 'This will permanently delete this lesson and all of its words. Are you sure?' },
            class: 'btn btn-danger btn-sm' %>      
      </li>
      <br>
    <% end %>
  </ul>

  <hr>
  <br>

  <h2>LESSONS I'M ENROLLED IN</h2>
  <ul>
    <% current_user.enrolled_lessons.each do |lesson| %>
    <li><%= link_to lesson.title, lesson_path(lesson) %>: <%= lesson.description %></li>
    <br>
  <% end %>
  </ul>
</div>

<br class="clear">
<br>